import { LaWalletConfig } from '@lawallet/react';
import { config } from './config';

function App() {
  return (
    <LaWalletConfig config={config}>
      {/** ... */}
      {/** ... */}
    </LaWalletConfig>
  );
}
